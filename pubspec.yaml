name: neodeter_pulse
description: Sistema de Marcaciones Neo Deter SAC
version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  connectivity_plus: ^5.0.2
  shared_preferences: ^2.2.2
  logger: ^2.0.2+1
  uuid: ^4.2.1
  location: ^5.0.3
  mobile_scanner: ^3.5.6
  permission_handler: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true